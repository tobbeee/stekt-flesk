apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  containers:
  - name: hello
    image: busybox
    command: ["sh", "-c", "echo hello world && sleep 3600"]

